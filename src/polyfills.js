// Web build: browsers provide DOMException and friends natively — nothing to
// polyfill. The React Native build substitutes polyfills.native.js (Metro
// resolves the .native variant automatically).
